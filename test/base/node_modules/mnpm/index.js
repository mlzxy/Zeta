var Zeta = require('../../../../'),
    chai = require('chai'),
    assert = chai.assert,
    expect = chai.expect,
    should = chai.should(),
    m = Zeta.module('mnpm', []);

m.fromNpmModule = true;
m.l();